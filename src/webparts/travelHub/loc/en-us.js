define([], function () {
  return {
    PropertyPaneDescription: 'Site-wide settings (list names, theme, hero image) live in Admin > Settings, not here.',
    BasicGroupName: 'General',
    DescriptionFieldLabel: 'Web part description'
  };
});
